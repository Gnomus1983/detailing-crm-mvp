# Demo Accounts Matrix - 2026-06-17

Проект: `detailing-crm-mvp`

## Зачем этот файл

В проекте больше нельзя держать demo-логины "по памяти".

Нужен один файл, где зафиксировано:

- кто есть кто
- какой логин для какой роли
- что реально стабильно
- что временно не считаем надёжным

## 1. Роли demo-контура

### Creator

- роль: `platform_admin`
- зона: только `/platform`
- не должен видеть обычную CRM компании как свой основной кабинет

Текущий известный логин:

- `arkhangel2016@gmail.com`

Статус:

- рабочий
- используется для creator / platform surface

### Owner

- роль: `owner` внутри компании
- зона: CRM компании
- должен видеть `dashboard / leads / clients / tasks / settings`

Текущий известный логин:

- `owner.demo.20260616@gmail.com`

Статус:

- рабочий
- зафиксирован в `.env` для smoke

### Manager

- роль: `manager`
- зона: CRM компании без owner settings

Текущий известный логин:

- `detailcrm.manager.1781099947936@gmail.com`

Статус:

- рабочий
- используется в smoke

### Detailer / Master

- роль: `detailer`
- зона: только назначенные заявки

Текущий известный логин:

- `test@detailcrm.com`

Статус:

- рабочий
- используется в smoke как базовый CRM-вход

## 2. Что считаем источником правды

Для автотестов:

- `.env`
- этот файл
- `tests/smoke.spec.ts`

Не считаем источником правды:

- старые handoff-заметки с разными owner email
- устные договорённости
- старые миграции с seed email, если auth-аккаунт уже менялся руками

## 3. Текущее правило для smoke

Сейчас:

- creator tests — гоняем
- manager tests — гоняем
- public tests — гоняем
- owner tests — гоняем через явные `OWNER_TEST_EMAIL` и `OWNER_TEST_PASSWORD` в `.env`

Это убирает ложный красный smoke из-за случайных старых fallback-логинов.

## 4. Следующий обязательный cleanup

Нужно отдельно сделать короткий проход:

1. подтвердить стабильность owner-входа в live smoke
2. при необходимости обновить `STATUS_TEST_TOKEN`
3. сверить owner-flow на `/settings` и `/status/:token`

## 5. Целевая матрица после cleanup

- creator: стабилен
- owner: стабилен
- manager: стабилен
- detailer: стабилен

После этого demo-flow и smoke больше не должны зависеть от случайных старых логинов.
